---
name: patent-disclosure-skill
description: 中国专利申请工程：面向结构设计已完成但样机、测试或量产尚未完成的提前专利部署，扫描项目材料，建立设计事实与验证状态台账，划分专利族边界，检索现有技术，执行审查员组合攻击，设计独立权利要求及回退层，直接撰写或迭代权利要求书、说明书和摘要，并生成样机阶段补强清单；也支持已有专利的通俗解读与 Obsidian 知识图谱。用于提前申请、专利挖掘、专利布局、申请文件撰写、权利要求策略、专利族边界、查新、对抗审查、申请文件迭代和专利解读。
---

# 中国专利申请工程与对抗审查

先选择模式，禁止把不同目标混在同一流程：

| 模式 | 适用任务 | 默认终点 |
|---|---|---|
| A · 样机前申请部署 | 结构方案已形成，样机/测试未完成，需要提前申请 | 可申报的申请文件包 + 样机补强清单 |
| B · 已有专利解读 | 已有公开号、PDF或全文，需要读懂 | 通俗笔记/知识图谱 |
| C · 申请文件迭代 | 已有权利要求、说明书或摘要，需要补充、纠错、改边界或续写 | 修订后的申请文件包 |
| D · 专利点与布局评审 | 尚不要求成稿，只评估发明点、查新、分案和权利要求策略 | 评审报告 |

本 Skill 不以技术交底书为中间产物或默认交付物。用户另行明确要求技术交底书时，根据已经确认的申请策略提取内容，不得让交底书模板反向决定申请边界。

## 基准规则

1. **设计事实可以支持申请，验证结果不能虚构。** 样机未完成不等于不能申请；只要结构、连接关系、动作链和替代方案足以使本领域技术人员实施，即可进入申请文件。不得把设计值写成实测值，把预期效果写成测试结论。
2. **四类证据分开管理。**
   - `DF`：已冻结的设计事实，可进入实施方式和权利要求；
   - `DV`：设计推导效果，可用“能够、可使、从而”谨慎表述；
   - `EV`：已有样机、仿真或试验验证；
   - `U/X`：待确认或相互冲突。
3. **独权保护技术关系，不保护项目愿望。** “提高效率、保持信息、避免差错、实现自动化”不能代替产生该结果的结构或步骤。
4. **未验证效果不作为创造性的唯一支柱。** 若区别贡献只能靠尚未取得的性能数据成立，应改选结构贡献、补充机理说明、设置回退层或降低申请判断。
5. **先做专利族，再写单案。** 已提交案件是固定边界；未提交案件可以重划。共享接口可以重复描述，但必须区分各案的技术问题、保护起点、保护终点和区别贡献。
6. **先攻击，后写独权。** 必须执行单篇覆盖、组合启示、公知常识、常规优化、功能性概括、规避设计和族内冲突攻击。
7. **公开充分性以“可实施”而非“已验证”为门槛。** 未完成样机本身不是阻断项；核心运动关系不明、结构互相干涉、关键参数只能靠试错获得且材料无范围、唯一实施方式无法运行，才是阻断项。
8. **申请文件不锁死当前样机。** 已冻结主方案写具体；有依据的替代结构、等效运动方式、数量变化和接口变体写入说明书，形成上位概括支撑和从属回退。
9. **检索时保护未公开信息。** 优先使用抽象技术特征，不向公开检索服务提交客户名称、内部编号、完整未公开方案或专利族策略。
10. **结论分级。** `可直接申报`、`修订后申报`、`不建议按当前主线申报`；不得用“未检到”冒充授权保证。

## 模式 A · 样机前申请部署

按顺序读取指定提示文件。已有答案不得重复询问。

### Step 1：部署边界

读取 `prompts/preprototype_intake.md`。

确认计划申请时间、已提交案件、未提交案件、公开时间线、结构冻结范围、未决设计项和期望交付范围。样机状态只用于控制表述和补强计划，不作为默认暂停理由。

### Step 2：全项目扫描与设计证据台账

读取 `prompts/project_scan.md` 和 `prompts/design_evidence_ledger.md`。

- 扫描本案、上下游接口案、系统案及已提交固定边界；
- Office 文件先转 Markdown；图中承担结构关系的信息必须人工核对；
- 标记 `DF/DV/EV/U/X`；
- 建立术语表、部件编号表、动作链、接口表和版本冲突表；
- 区分当前主方案、可实施替代方案、待选方案和已否定方案。

**门禁 G1**：若无法确认核心部件之间的位置、连接/配合或动作链，输出阻断问题；不得通过文字补造闭环。

### Step 3：发明构思与专利族架构

读取 `prompts/patent_points_analyzer.md` 和 `prompts/family_architecture.md`。

- 从结构制约关系中提炼候选发明构思，不以“模块集合”命名发明点；
- 为每案确定保护起点、终点、必要接口、非本案内容；
- 建立“已提交固定边界—未提交可调整边界—共同接口—区别贡献—重复保护—保护空洞”矩阵；
- 对近似案件决定合并、拆分、母子系统布局或同日协调申请。

### Step 4：检索与逐特征对比

读取 `prompts/prior_art_search.md`。

- 先检索宽概念，再检索核心关系、关键运动和替代结构；
- 区分申请日前现有技术、抵触申请线索、申请日后材料和族内文件；
- 阅读相关文献的摘要、独立权利要求及支持核心特征的说明书段落；
- 制作逐特征对比，不以标题或场景不同作结论。

**门禁 G2**：没有可解释的最接近现有技术或只有标题级结果时，只能给出检索不足结论，不能给出稳定性判断。

### Step 5：审查员攻击与工程可实施性检查

读取 `prompts/adversarial_examination.md` 和 `prompts/preprototype_enablement.md`。

分别检查：

1. 单篇覆盖；
2. 最接近现有技术与第二文献/公知常识的组合；
3. 常规尺寸、数量、驱动方式和自动化替换；
4. 功能性上位概括是否有多个结构实施方式支撑；
5. 当前结构是否形成不矛盾的运动链；
6. 样机未验证事项是否被错误写成确定性能；
7. 竞争者删除、替换、移位或改变传递方式后的规避路径；
8. 族内重复授权、内部在先申请和边界空洞。

**门禁 G3**：核心结构不能据材料实施，或区别贡献只能依赖未验证且无法从结构推出的效果时，不得进入可申报稿。

### Step 6：权利要求架构

读取 `prompts/claim_strategy.md`。

- 先定义发明构思，再写独权；
- 对每个特征执行删除测试与规避测试；
- 形成 `C` 核心必要、`D` 抗攻击强化、`E` 从属/实施方式扩展三层；
- 同时设计宽、中、窄三档独权候选及至少两层回退；
- 对装置、系统、方法分别判断是否具有独立贡献，不为数量强写多类型独权；
- 明确说明书必须增加的等效方案和术语上位概括。

### Step 7：申请策略确认门

读取 `prompts/filing_preview.md`。

展示单案边界、拟独权、回退层、最强组合攻击、族内关系、未验证效果及处理方式。只询问会改变申请边界或独权的事项；普通样机细节进入后续补强清单。

除非用户已明确批准相同主线或要求直接成稿，否则等待确认。

### Step 8：直接撰写申请文件

读取 `prompts/application_builder.md` 和 `prompts/filing_package.md`。

按“权利要求骨架 → 说明书支撑 → 权利要求定稿 → 摘要”的顺序撰写：

- 权利要求书；
- 说明书：技术领域、背景技术、发明内容、附图说明、具体实施方式；
- 说明书摘要；
- 内部工作底稿：检索对比、族边界、风险和样机补强清单。

只把 `DF/EV` 写成确定事实；`DV` 只能写为结构可推导的作用；`U/X` 不得暗中补齐。申请文件不得写“已经试验表明”，除非存在 `EV`。

### Step 9：申请文件双重自检

读取 `prompts/application_self_check.md`。

依次以代理人、审查员和规避者视角检查：

- 独权是否具有最小必要技术关系；
- 每一项权利要求是否有说明书原文级支撑；
- 背景技术是否无意承认过多现有技术；
- 发明内容、实施方式、附图和权利要求是否同版；
- 未验证性能是否被确定化；
- 上位概括是否覆盖至少一个清楚实施方式，并尽量具有替代方式支撑；
- 多案边界是否一致；
- 编号、引用、术语和动作链是否无冲突。

交付等级：

| 等级 | 条件 |
|---|---|
| 可直接申报 | 核心结构可实施，独权有支持，主要族边界和检索风险已处理 |
| 修订后申报 | 主线成立，但存在可明确关闭的文本、附图、边界或支持问题 |
| 不建议按当前主线申报 | 核心结构不清、区别贡献不成立或依赖无法支撑的未验证效果 |

### Step 10：样机阶段补强闭环

读取 `prompts/prototype_validation_plan.md`，输出与申请正文分离的样机补强和后续申请计划。不得承诺把申请日后形成的新内容补入原申请；新增技术内容应评估另行申请。

## 模式 C · 申请文件迭代

读取 `prompts/iteration_context.md`，再按意图读取 `prompts/merger.md` 或 `prompts/correction_handler.md`。重新建立受影响部分的 `DF/DV/EV/U/X` 台账。修改权利要求时必须同步检查说明书支撑、发明内容、摘要、附图标记和专利族边界；修改实施方式时必须反查权利要求是否仍覆盖。不得只做局部文字替换。

若用户给出审查意见，先重建审查员的特征映射和组合逻辑，再决定争辩、修改或分案；不得先写答复措辞。

## 模式 D · 专利点与布局评审

执行模式 A 的 Step 1–7，输出评审结论，不进入申请文件成稿。用户要求“先分析”“对抗审查”“划分边界”时默认使用本模式。

## 模式 B · 专利通俗解读

读取 `prompts/patent_plain_reader.md`。先运行 `tools/patent_reader/check_obsidian_env.py`；只有公开号时使用 `tools/patent_reader/fetch_patent_pdf.py`。随后提取全文和附图，校对权利要求树，制作特征—说明书—附图对照，并按需生成 Obsidian 笔记和知识图谱。

需要写入 Obsidian 时，再读取：

- `prompts/obsidian_ofm_companion.md`
- `references/patent_obsidian_format.md`
- `assets/patent_note_template.md`
- `prompts/patent_reader_self_check.md`

## 文件与工具

- Office 转换、CNIPA、Mermaid、公式及 Word 工具见 `tools/README.md`；工具不存在时记录失败并使用可核验替代方案，不得引用不存在的脚本。
- 对外检索只发送抽象特征；不得上传未公开全文。
- 申请文件默认命名 `{案件名称}_{文件类型}_{YYYYMMDDHHmmss}.md/.docx`，不覆盖旧稿。
- 检索记录、设计证据台账、攻击矩阵、专利族矩阵和样机补强清单为内部工作底稿，与申请正文分离。
- 样机前申请文件无需伪装成已验证成品；真实性、可实施性和申请边界优先于文稿完整感。
